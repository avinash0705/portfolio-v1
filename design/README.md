# design/

**Purpose:** Non-deployed design source material — diagrams, sketches, exports, logo/source artwork, and other working design files.

Nothing in this directory is shipped with the application. Deployable, servable assets live in `portfolio/public/` instead, per `docs/021-folder-structure.md`.
